name: pdf_app
description: A smart PDF editor and viewer
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  syncfusion_flutter_pdfviewer: ^24.1.41
  file_picker: ^8.0.0
  shared_preferences: ^2.2.3
  pdf: ^3.10.8
  path_provider: ^2.1.1
  image: ^4.1.7 # फोटो को 90 डिग्री घुमाने के लिए नई लाइब्रेरी

flutter:
  uses-material-design: true
