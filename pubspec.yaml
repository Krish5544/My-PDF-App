name: pdf_app
description: A smart PDF editor and viewer
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  syncfusion_flutter_pdfviewer: ^24.1.41
  file_picker: ^8.0.0

flutter:
  uses-material-design: true
